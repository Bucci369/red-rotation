---
// Und auch hier...
export interface Item {
    type: 'image' | 'video';
    src: string;
    alt: string;
}
export interface Props {
    items: ReadonlyArray<Item>;
}
const { items } = Astro.props;
---
<section class="sport section" id="sport">
    <h2 class="section-title serif scroll-reveal">Sport & Lifestyle</h2>
    <div class="sport-grid">
        {items.map((item, index) => (
            <div class={`sport-item portfolio-item-reveal stagger-${index + 1}`}>
                {item.type === 'video' ? (
                    <video autoplay loop muted playsinline class="portfolio-video" data-smart-video>
                        <source src={item.src} type="video/mp4" />
                    </video>
                ) : (
                    <img src={item.src} alt={item.alt} loading="lazy" decoding="async" />
                )}
            </div>
        ))}
    </div>
</section>